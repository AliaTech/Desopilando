<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.desopilando.aliatech.desopilando">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/LoginTheme">
        <activity android:name=".activity.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.activity.MainActivity" />
        <activity android:name=".activity.activity.CadastroUsuarioActivity" />
        <activity android:name=".activity.activity.CadastroEstabelecimentoActivity"></activity>
    </application>

</manifest>